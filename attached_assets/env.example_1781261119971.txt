# Copy these into Replit "Secrets" (lock icon in the sidebar). Do NOT commit real keys.

# From Stripe Dashboard → Developers → API keys
STRIPE_SECRET_KEY=sk_live_xxx

# From Stripe → Products → your "Lectio Monthly" $6/mo recurring price → price ID
STRIPE_PRICE_ID=price_xxx

# From Stripe → Developers → Webhooks → your endpoint signing secret
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Any long random string (used to sign access cookies). Generate once, keep it.
COOKIE_SECRET=replace-with-64-random-characters

# Your public Replit URL once deployed, e.g. https://lectio.yourname.repl.co
BASE_URL=http://localhost:3000
